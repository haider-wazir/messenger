development:
  adapter: async

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") %>
  ssl: true
  ssl_params:
    verify_mode: <%= OpenSSL::SSL::VERIFY_NONE %>
    ssl_version: :TLSv1_2
  channel_prefix: messenger_production
